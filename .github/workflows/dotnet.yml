# File: .github/workflows/dotnet.yml
name: .NET Build & Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET 8
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    # Run all commands from the backend folder where the .sln actually lives
    - name: Restore dependencies
      working-directory: backend
      run: dotnet restore PersonalMediaTracker.sln

    - name: Build (Release)
      working-directory: backend
      run: dotnet build PersonalMediaTracker.sln --no-restore --configuration Release

    - name: Run tests
      working-directory: backend
      run: dotnet test PersonalMediaTracker.sln --no-build --configuration Release --verbosity normal
